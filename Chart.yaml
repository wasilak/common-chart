apiVersion: v2
name: common
description: A comprehensive, production-ready Helm chart for deploying applications on Kubernetes
type: application
version: 1.2.0
appVersion: "1.2.0"
home: https://github.com/wasilak/common-chart
sources:
  - https://github.com/wasilak/common-chart

maintainers:
  - email: piotr.m.boruc@gmail.com
    name: wasilak

annotations:
  artifacthub.io/changes: |
    - kind: added
      description: "Add support for mounting Kubernetes Secrets as volumes with volumes.secret configuration"
    - kind: changed
      description: "Updated values.schema.json with secret volume properties and configuration options"
